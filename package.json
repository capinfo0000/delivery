{
  "name": "tiktok-comment-tts",
  "version": "1.0.0",
  "private": true,
  "description": "TikTok LIVE のコメントを音声読み上げするアプリ（Web / iOSネイティブ両対応）",
  "type": "module",
  "scripts": {
    "copy:web": "node scripts/copy-web.mjs",
    "sync": "npm run copy:web && cap sync",
    "ios:add": "npm run copy:web && cap add ios",
    "ios:sync": "npm run copy:web && cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor-community/text-to-speech": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
